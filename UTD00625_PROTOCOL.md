# Five-task UTD ablation

Compare DAWN UTD0.0625 against the five completed UTD0.25 warmup80k/online150k runs. Train one seed (0) for each cube-double-play-singletask task1–5, with each task's identical QAM offline500k checkpoint and inherited current/target critics, fresh optimizers and frozen base policy. Only the gradient-update frequency changes: target updates are floor((primitive environment steps - 80000) / 16), instead of division by4. The total is4375 updates rather than17500.

Reuse each reference run's exact saved base-only warmup collector checkpoint at80002–80004 steps. Load the same replay, environment reconstruction trace, observations, episode counter, base and residual random keys, NumPy random state and zero-update initial agent. Warmup occupies the first80k of the150k budget; it is not added on top. Do not continue from a trained online agent. The reused prefix avoids unnecessary recollection and improves pairing. After updates start, collected trajectories and replay naturally depend on the changed policy and need not remain identical; this is an online UTD ablation, not a fixed-dataset learning experiment.

Unchanged settings: ordinary/naive TD target (no critic entropy bonus), actor entropy and automatic temperature retained; batch256, residual scale0.1, state37+sampled base action chunk25 input, horizon5, online-only growing uniform replay,10 critics, minimum actor-Q/target aggregation, residual MLP3x256 ReLU with tanh-Gaussian outputs, critic4x512 GELU+LayerNorm, lr1e-4, discount0.99, tau0.01, gradient clip50, initial alpha0.01, entropy target-25. QAM edit_scale0 and inv_temp1. Same installed dependencies, RTX4090, EGL library, disabled CUDA command buffer, GPU memory fraction0.38, maximum2 simultaneous processes.

Evaluate the identical100 reset seeds and policy random seeds per task at0, approximately100k,120k and150k environment steps. Mean residual is primary; sampled residual secondary; base remains sampled in both. The0k evaluation is reused exactly from the unchanged warmup collector. There is no separately measured80k evaluation. Report measured intermediate steps rather than forcing their few-step chunk offsets to match. Nominal update counts at100k,120k,150k are1250,2500,4375.

Preserve all historical source files. A separate wrapper records all changes against the prior task2/3/4 wrapper in a reversible manifest. The runtime residual function is copied from immutable run.py; an AST check guarantees its only executable change is the UTD multiplier0.25 to0.0625. Save that exact executed function per run. Verify inherited full-agent hash and actual configuration against the corresponding UTD0.25 run, allowing only UTD to change. Verify warmup checkpoint and replay hashes, exact simulator reconstruction, fixed evaluation identities, frozen base, finite metrics, expected update counts, and final checkpoint hashes.

The supervisor first runs a task1 smoke experiment (80 warmup/200 total steps,2 evaluation episodes,7 updates), then dispatches all five formal tasks. Outputs live at runs/utd00625_warmup80k_150k_20260911, physically under /root/qam_dawn_outputs on the system disk. Results include both settings, final comparison and episode-paired gained/lost counts. This is one training seed; evaluation episodes do not measure training-seed variability. Compare at the same environment budget, explicitly showing different gradient-update budgets.
