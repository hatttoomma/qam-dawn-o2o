# Launch correction: complete-chunk warmup boundary

The first formal launch reached the identical warmup boundary in both arms at primitive step 20,003, with zero completed updates in the last progress records. The supervisor incorrectly asserted that warmup must end at exactly 20,000, then terminated both workers before the first training checkpoint. The unchanged historical collection loop completes the current action chunk before enabling residual actions; its previous Task5 warmup also ended at 20,003.

Correct the supervisor check to require equal actual warmup steps in [20,000, 20,004], equal warmup replay hashes, and exact agreement with the historical Task5 warmup. Preserve the old supervisor source and source manifest with a correction audit.

The next attempt reused cached offline evaluations and produced unequal warmup replay byte hashes despite matching boundary steps and chunk counts. Both warmup records were freshly written, so this was not a comparison against a stale record. Its exact numerical cause is unresolved. Preserve this attempt under `interrupted_before_updates`, then restart both arms in fresh output directories, including fresh offline evaluation, matching the original launch procedure. No online training state is reused. The training loop, agent implementation, hyperparameters, evaluation seeds, and final update budget remain the protocol's original values. Require successful warmup equality checks and actual gradient updates before reporting successful launch.

A further attempt with fresh evaluations also produced unequal warmup byte hashes, so evaluation caching is not an established explanation. The final launch uses the protocol's shared-prefix amendment: one frozen-base collector produces a warmup checkpoint with zero updates, then both independently initialized online agents receive the identical replay, simulator reconstruction state and RNG state. A new short-run check verifies branching and exact resume. Final run directory: `runs/task5_long_critic_shared_20260910`. The earlier `runs/task5_long_critic_20260910` directory contains interrupted attempts, not completed long-budget results.

Successful launch verified at 2026-09-10 08:19:04 UTC: inherited-Q worker 31720 reached 26,000 steps / 1,500 updates; random-Q worker 31721 reached 26,001 steps / 1,500 updates. Both were alive with finite logged losses and gradients. Shared warmup byte hash also equals historical Task5 warmup hash `d0f6e1da2ce975d8a933c63a8d802315ea94bb619af9cfdd0c29213cc9ae87d0`. The short shared-prefix run and its interrupted/resumed counterpart matched exactly, including agent, replay, episode trace, observation, and RNG states. Baseline evaluation: 76/100 success, average return -431.09.
