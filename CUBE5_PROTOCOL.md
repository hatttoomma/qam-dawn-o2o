# Cube-double five-task expansion

Authorized: all five cube-double-play-singletask tasks, seed 0, 500,000 offline updates and 50,000 primitive online environment steps. Extend the existing experiment; reuse completed, matching Task1/2 runs after checking their provenance. Do not choose a checkpoint or evaluation repeat by its score.

QAM-EDIT baseline: use the paper's selected cube-double configuration for all five tasks: inverse temperature 1 and edit scale 0. Source: https://arxiv.org/html/2601.14234v1#A5, Table 5. Table 6 includes zero in the QAM-EDIT search and explicitly identifies it with vanilla QAM. Thus this is the official selected QAM-EDIT configuration, with no active editor. It is not a newly tuned best configuration for our smaller budget. Keep the previously pinned QAM commit 2726d767c9a0a7a46d49693f0391f73dc2cf58ac and all native training settings. QAM native online updates the prior, flow policy and critic; batch 256, UTD 1, update start 5k, 45,001 updates, uniform offline+online sequence replay, learning rate 3e-4, target tau .005, target mean minus .5 std, actor Q mean, joint gradient clipping 1.

DAWN-derived online method: frozen non-edit QAM base, inherited current and target critic, ordinary (non-entropy) TD target, batch 256, UTD .25, state plus actual sampled base action chunk as actor input. Input 37+25=62, 3x256 ReLU, Gaussian mean and log-standard-deviation heads, scale .1, online-only chunk replay, 20k base-only warmup included in 50k, 7,500 updates, learning rates 1e-4, tau .01, min over 10 critics for actor and target, gradient clipping 50. Actor entropy and automatic alpha remain enabled, initial alpha .01 and target entropy -25. Use the exact previously tested actor_input_agent.py, hard_backup and diagnostics. No new hyperparameter search.

Both methods use horizon 5, discount .99 per primitive step, the same task-specific offline500k checkpoint, unmodified OGBench task reward/masks, and independent training/evaluation RNG. Task3,4,5 each get a fresh task-specific offline run; sharing the play dataset does not mean sharing their learned critic or policy. Reuse Task1/2 offline checkpoints, native final results, and the immediately preceding base_action DAWN results. Preserve their hardware/source provenance, including the older native run runtime. Reuse does not add new training seeds.

Before formal runs: audit task IDs, target positions, task-specific reward and mask relabeling, shared state/action data and dataset hashes for all five tasks. Run 32-update offline and 60-step online smokes on each new task, with a 20-step residual warmup and a 20-step native update start. Verify inherited task-specific critic/target, frozen base for DAWN, native flow updates, actor input and gradient checks, hard-TD math, finite metrics, exact update counts and equal initial evaluations. Freeze all training/protocol sources. Maintain resumable checkpoints and fail visibly; never overwrite historical runs.

Primary endpoint: success rate and average raw return over the final 100 fixed-reset evaluation episodes, using sampled policies. Online curves use 50 episodes at seven prespecified checkpoints (0,5k,10k,20k,30k,40k,50k), recording actual chunk-aligned steps. Final mean-residual evaluation is a secondary 50-episode diagnostic, not a different training algorithm. Report equal-weight five-task averages and each task individually, including all outcomes. This remains one training seed per task and method, with no across-seed confidence claim. Check frozen source hashes, checkpoint hashes, task/data provenance, update counts, raw episode aggregates and evaluation state integrity before exporting the report.
