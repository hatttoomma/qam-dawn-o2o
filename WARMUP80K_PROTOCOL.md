# Task1 / Task5: 80k warmup, 150k total online steps

Requested 2026-09-11. Start fresh online runs from the same task-specific QAM offline500k checkpoints used in the earlier experiments; do not resume the 1M online agents. Training seed=0. Critic initialization is explicitly recorded in `suite_config.json` (inherited by default, matching the five-task main experiment).

The requested changes are base-only warmup from 20k to 80k primitive environment steps and total online budget to 150k. Warmup is included in that total: the remaining 70k steps produce exactly 17,500 gradient updates at UTD=0.25. During warmup, neither actor nor critic is trained, and no residual is applied. A chunk crossing 80k finishes normally (actual boundary 80,000–80,004); fractional update accounting catches up after residual training begins. Preserve the original `run.py` collection/update loop without edits.

Unchanged: ordinary TD (no entropy bonus in the critic target), actor entropy and automatic alpha enabled, batch256, state+actual sampled base chunk (37+25=62), 3×256 ReLU tanh-Gaussian residual, scale0.1, 10 critics with 4×512 GELU+LayerNorm, minimum Q for actor and target, growing online-only uniform replay, actor/critic/alpha learning rate1e-4, target tau0.01, gradient clip50, initial alpha0.01, target entropy−25, horizon5, primitive discount0.99, frozen QAM base with edit_scale0 and inv_temp1. Inherited runs copy both current and target offline Q and use fresh optimizers; random runs, if selected, initialize current Q randomly and set target Q equal to it.

Task1 offline SHA256: `f318b912d53af2d18f6dbf6ca2815a0af8fb0a795afc325f1544b81b1f080cc1`.
Task5 offline SHA256: `fc0ac82260d16262da8e0c1d7266fd19fc024dfc52c4b40b26e64b6463a1938a`.

Each task first collects its full base-only warmup using the unchanged loop. Its replay, episode trace, observation, rollout RNG keys and NumPy RNG state are restored at the complete-chunk boundary into its freshly initialized online agent. This is the same shared-prefix mechanism already validated for Task5 long training; it also permits an exact common warmup if both critic initializations are requested. No extra warmup environment budget is charged to a logical run, and no gradient updates are repeated. Save and audit the simulator restoration error.

Evaluation: 100 fixed reset seeds500000–500099, base RNG seeds800000–800099, residual RNG seeds900000–900099. Evaluate the offline baseline and checkpoints at100k,120k,150k using both mean residual and sampled residual. Mean residual is the primary reporting mode; base remains sampled. The base-only80k collection checkpoint has the unchanged offline policy, and its evaluation is reused from0k instead of running an identical extra rollout. Report this reuse explicitly rather than claiming a new measurement. Retain slim checkpoints and full latest checkpoints with optimizer/replay/RNG state. No selection of checkpoints or evaluation modes based on score.

Before formal collection, validate both tasks using80 warmup /200 total steps (30 updates), check task-specific data/Q hashes, frozen base, hard TD and actor input, then compare uninterrupted and interrupted/resumed runs at a complete-chunk checkpoint. The original protocol and source hashes remain immutable. The new machine uses its own installed NVIDIA EGL library; this is a runtime path adjustment, not an algorithmic change.

Outputs: `runs/warmup80k_150k_20260911/`. The launcher writes `PREFLIGHT_PASSED.json`, per-stage process events, per-run progress, final checks and `results.json`/`results.csv`. This is one training seed per task and configuration; do not infer across-seed robustness. Changing both warmup and total budget is a combined schedule experiment, not an isolated warmup-only comparison.

Runtime amendment before formal training: the new host runs NVIDIA driver535.146.02, while the cloned CUDA runtime is12.9.79. Both initial smoke runs failed at the first optimizer update during CUDA graph capture (`CUDA_ERROR_SHARED_OBJECT_INIT_FAILED`); collection, initialization hashes and simulator reconstruction passed. Preserve that attempt separately. Set `XLA_FLAGS=--xla_gpu_enable_command_buffer=` for this suite to disable graph capture and use ordinary GPU kernel launches. Keep the same model, losses, dependencies and optimizer hyperparameters, then rerun the complete preflight. Record this runtime difference from earlier machines explicitly.

Resume-check amendment: the uninterrupted and pause/resume Task1 smoke runs have identical initial agent and warmup replay, but their agent hashes already differ at the124-step checkpoint before the interruption. Their final200-step weights and one evaluation return differ slightly. Thus final bitwise equality between these independent GPU runs is not a valid isolated restore check on this runtime. Preserve these runs and report this limitation. Instead, validate each complete checkpoint through the actual Flax agent loader, requiring exact model/optimizer/replay/RNG restoration and zero simulator reconstruction error; all short runs still must reach exactly200 steps/30 updates with finite metrics and both evaluation modes. Do not claim independent GPU trajectories are bitwise deterministic.
