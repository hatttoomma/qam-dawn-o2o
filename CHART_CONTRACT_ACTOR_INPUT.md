# Actor input comparison figure

Question: does exposing the sampled base action improve the residual policy at fixed plain TD, batch 256 and UTD .25? Use paired, freshly trained 62-input networks, with zero-masked versus real base features. Task1 and Task2 each have seven prespecified 50-episode checkpoints and a separate final 100-episode evaluation. No denser evaluations exist, so use unconnected ordered dots, without smoothing or interpolated curves. State-only masked control: open blue squares; real base input: filled darker blue circles. Two task rows and two columns for success and return. Show the 20k warmup boundary, success axes 0–100%, clearly labeled return axes, sample counts and single training seed. Export standalone Matplotlib PNG and PDF. Inspect the final image and verify consistency with the CSV. Primary endpoint table uses 100 episodes; no across-seed uncertainty bands.
