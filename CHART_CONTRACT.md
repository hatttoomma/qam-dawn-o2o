# Research figure contract

- Question: how do the requested three complete online procedures compare over 50k primitive interactions after the shared 500k offline checkpoint?
- Takeaway: fill only from validated completed evaluations; none is asserted before training finishes.
- Surface: standalone scientific PNG/PDF generated by Matplotlib, following the runtime instruction to use standard plotting tools for research figures. Accompany with exact CSV and Markdown table.
- Two panels: success percentage and episode return against online environment steps. Seven preplanned evaluations per arm; connect marked points only, without smoothing or invented intermediate measurements. A finer measurement query is unavailable because evaluations occur only at the fixed protocol grid. Exact values and episode counts remain in the table; interpolation is used only for explicitly labeled trapezoidal AUC.
- Palette: hard two-root cap plus neutral. Warm DAWN blue #2463A6, random DAWN gold #B68420, baseline grey #53575D. Distinguish with markers and solid/dashed lines as well as color.
- Three methods are the user-requested complete comparison; no extra method is introduced for visual density. Each series has seven intentional budget checkpoints.
- Footprint: 12.5x4.8 inches. Export learning_curves.png at 180 dpi and vector learning_curves.pdf. Inspect PNG at full resolution before delivery.
- No across-seed confidence bands for this single-seed pilot. Final 100-episode scores appear in the exact table, separately from primary 50-episode curves.
- Include warmup in the x-axis budget and label the 20k DAWN warmup boundary. Keep offline checkpoints and online checkpoints on separate axes/artifacts.
- Source: per-episode evaluation JSON; verify aggregates and checkpoint provenance before plotting. No external dataset upload or hosted dashboard is required.
