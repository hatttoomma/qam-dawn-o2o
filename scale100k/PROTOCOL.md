# AntMaze-large and Cube-double:100k online expansion

Authorized scope: tasks1–5 in each benchmark, seed0, QAM native and DAWN;20 logical runs. Reuse each original task-specific offline500k checkpoint. QAM native settings remain benchmark-specific: antmaze horizon1/inv_temp10; cube horizon5/inv_temp1; both edit_scale0. Both final online budgets100k primitive steps.

DAWN schedule:40k base-only warmup, no updates;40k–50k use exactly128 offline+128 online per batch256 and UTD0.25;50k–100k use256 online/0offline and UTD0.0625. Cumulative updates2500+floor((step−50000)/16) after50k, hence5625 at100k. Preserve the whole growing online buffer, including warmup and mixed-phase rollouts. Frozen base, inherited current and target Q, naive TD, state+sampled base action, scale0.1,10critics/min actor and target aggregation, same network/optimizer/alpha settings as previous experiments. Actor entropy and automatic alpha remain enabled.

Antmaze DAWN resumes exact complete60k states for tasks1,2,3,5 (3125 updates), and exact50k balanced state for task4 (2500 updates). Existing optimizer, temperature, RNGs, replay and environment are restored. New final100k evaluation only; previous evaluated records are retained.

Antmaze native old outputs lack online replay/environment state. Reuse the same offline500k agent including optimizer, then run a new100k online stage with the pinned, unchanged official main and original hyperparameters. This is a fresh online seed0 run, not an exact continuation of the previous50k trajectory. The execution flag offline_steps0 means pretraining is reused, not absent. Official native UTD1/start5000 gives95001 online updates. No offline retraining. Final paired100episode evaluation; reused offline evaluation. New final replay/model/RNG and partial environment data are saved, but any future restore must validate environment completeness.

Cube native restores each complete original50k checkpoint including optimizer, replay, action queue, RNG and simulator trace; continues50k more steps to95001 cumulative updates. Cube DAWN has no matching40k+mixed50k state, so starts from the original task-specific offline500k checkpoint. Action chunks have5 primitive steps; warmup/switch checkpoint boundaries can fall up to4 steps after nominal40k/50k when a chunk crosses them. The cumulative update target is anchored to nominal boundaries; first2500 updates use mixed replay, subsequent3125 use online-only. Final100k is exact.

Cube offline replay samples uniform sequences through the pinned QAM sampler. For residual SMDP critic inputs, accumulate rewards through valid steps, bootstrap with gamma^actual_valid_length times the terminal mask, clamp next observation to the last valid transition, and zero-pad the unexecuted action suffix after termination/truncation (do not import next-episode actions). Current/next frozen-base features are resampled per update via a separate fold-in JAX stream. Online replay remains full issued chunks with actual SMDP rewards/discounts. Synthetic termination/truncation conversion is checked in smoke runs. This horizon5 adapter is necessary because the previous mixed-buffer experiment used antmaze horizon1.

Cube evaluation:100 fixed paired episodes for native; DAWN mean residual primary and sampled residual secondary. Evaluate fresh DAWN0,50k,100k (40k unchanged policy reuses0); resumed native new100k only. Preserve training RNG around evaluation. Cube runtime retains the previously required disabled CUDA command buffer and installed NVIDIA EGL library. OMP, OPENBLAS and MKL thread counts remain1, matching the historical launcher. A rejected smoke with4threads changed initial observation hashes; restoring1thread exactly matched the original evaluation records and simulator restoration (max observation error0).

Machine45557 hosts antmaze: DAWN suite then native suite share an exclusive GPU lock (short native smoke can run before acquiring the production lock). Machine39459 hosts cube, sequential native/DAWN pairs after both short smokes. New cube outputs use /root/qam_dawn_outputs/scale100k_20260914 on the system disk (24GB initially free), avoiding deletion of old checkpoints. No checkpoints have been deleted.

All runtime/model inputs are hashed, short smokes gate formal runs, complete source states and actual update counts are checked. Single seed:do not infer across-seed stability. Compare per-task success/return at100k and report actual gradient-update counts95001 vs5625; gradient iterations are not equal FLOPs/wall time. Preserve all measured failures including task4. Never relabel older results as newly run settings.

## Launch and monitoring

All four short smokes (antmaze native/DAWN, cube native/DAWN) passed on2026-09-14. Antmaze DAWN suite PID69899 started formal task1 at11:08:29UTC. Antmaze native suite PID72158 passed smoke and waits for the shared GPU lock. Cube suite PID2100 passed both smokes and started formal native task1 at11:14:20UTC. PIDs are launch references only; check live state.

Local `fetch_status.py` reads the three queues on two hosts without storing credentials. Use `--full` for episode records, source fingerprints and final checkpoint SHA256 verification. `validate_completed.py` audits completed runs and writes `output/scale100k_20260914/validation_summary.json`, `completed_results.csv` and `results.md`. It checks update counts, replay audits, restoration, frozen base, evaluation aggregation and final native/DAWN paired initial states. An SSH error alone is not a training failure. Do not restart partial runs or change parameters automatically. Historical rejected smokes are archived separately from active results and are not performance observations.

Reward validation is benchmark-specific. Antmaze episode return equals minus length plus terminal success. CubeEnv.compute_reward uses sum(object successes) minus2, giving per-step reward -2/-1/0; Cube return can be more negative than minus episode length. The local endpoint validator initially used the Antmaze identity for Cube and was corrected after checking the installed OGBench source. No training or evaluation data changed; Cube aggregation is checked against all100 episode returns and source-supported reward bounds.

On2026-09-14 at12:31:31UTC, the first Antmaze native formal attempt exited at5k after its first gradient update: official CsvLogger adds `step` to the metric dictionary in place, causing duplicate keyword arguments in the extra JSON logger. The failed attempt and exact source were archived at `attempts/logging_failure_20260914_123131`; no formal native result existed. The adapter now snapshots metric fields before calling the unchanged official logger. Formal hyperparameters, update loop, dataset and checkpoints are unchanged. A local regression exercised5k/6k/100k logging, and the isolated24step smoke now logs at8/16/24 to exercise the actual CSV mutation. New suite PID79979 launched at12:40:44UTC and restarts fresh online0 from the complete original offline500k checkpoint after smoke; it does not resume the incomplete failed state. Live source hashes and `logging_repair.json` preserve this repair provenance. Historical failed attempts are excluded from current results, but retained for auditing.

The revised logging smoke passed with returncode0 and logged updates1/9/17 at steps8/16/24. Formal native task1 restarted at12:41:43UTC (initial childPID81096). Antmaze DAWN all5tasks completed and exited normally at12:30:20UTC.

Antmaze native endpoint replay export limitation: the pinned official ReplayBuffer.add_transition increments its pointer modulo capacity and then sets size=max(pointer,size). With capacity offline_size+online_steps, the final write wraps pointer to0 and leaves size=capacity-1, although the last physical slot was written. The unchanged official final update samples using that size. The adapter's exported slice therefore contains99999 online transitions at100k; the physical final transition is omitted. Actual env steps100000 and model updates95001 are verified separately. Do not modify this official training behavior or rerun models because of this counter; final performance remains valid. Together with the already partial simulator export, native latest.pkl is NOT an exact continuation state. This limitation applies to all5 Antmaze native100k runs; a future extension must address it explicitly rather than silently restoring the incomplete export. Cube native uses its original different collector and is unaffected. The local validator checks99999 exported records,100k final logged steps and95001 model/logged updates.
